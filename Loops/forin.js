const myObj = {
    name: "ADARSH JHA",
    age: 18,
    education: "BCA",
    height: "5'9"
}

// for (const key in myObj) {
//     console.log(`the key and values are: key -> ${key} & value -> ${myObj[key]}`);
// }

let arr = [1,23,4,5,6,78,7]
for (const key in arr) {
    console.log(arr[key]);
}